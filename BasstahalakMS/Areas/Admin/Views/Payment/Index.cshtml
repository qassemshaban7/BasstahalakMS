@model IEnumerable<Payment>
@using System.IO

@{
    ViewData["Title"] = "Index";
    Layout = "~/Areas/Admin/Views/Shared/_Layout.cshtml";
}

@section head {
    <!-- BEGIN PAGE LEVEL STYLES -->
    <link rel="stylesheet" type="text/css" href="~/lib/table/datatable/datatables.css">
    <link rel="stylesheet" type="text/css" href="~/lib/table/datatable/dt-global_style.css">
    <!-- END PAGE LEVEL STYLES -->
}

<div class="br-pagetitle">
    <i class="icon fas fa-light fa-cog"></i>
    <div class="row">
        <div class="col-lg-10 col-10">
            <h3 class="text-info">قائمة المدفوعات</h3>
        </div>

        <div class="col-lg-2 col-2">
            <a asp-action="Create" asp-controller="Payment" class="btn btn-primary">اضافة فاتورة جديدة</a>
        </div>
    </div>
</div>
<div class="br-pagebody">
    <div class="br-section-wrapper">
        <div class="table-wrapper">
            <i class="fas fa-file-pdf"></i>
            <table id="paymentTable" class="table display responsive nowrap">
                <thead>
                    <tr>
                        <th>اسم المطبعة</th>
                        <th>المبلغ</th>
                        <th>نوع الدفع</th>
                        <th>وقت الدفع</th>
                        <th>دليل الدفع</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var payment in Model)
                    {
                        var paymentTypes = new Dictionary<int, string>
                    {
                    {1, "فوري"},
                    {2, "كاش"},
                    {3, "فودافون كاش"},
                    {4, "اتصالات كاش"},
                    {5, "فيزا"}
                    };
                        <tr>
                            <td>@payment.User.FullName</td>
                            <td>@payment.Money</td>
                            <td>@paymentTypes[payment.Type]</td>
                            <td>@payment.PaymentTime</td>
                            <td>
                                @if (System.IO.Path.GetExtension(payment.PhotoPath)?.ToLower() == ".pdf")
                                {
                                    <embed src="@Url.Content(payment.PhotoPath)" type="application/pdf" width="300" height="200" />
                                }
                                else if (!string.IsNullOrEmpty(payment.PhotoPath))
                                {
                                    <img src="@Url.Content(payment.PhotoPath)" alt="فاتورة" width="150" height="150" />
                                }
                                else
                                {
                                    <span>كاش</span>
                                }
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>
@section scripts {
    <!-- BEGIN PAGE LEVEL SCRIPTS -->
    <script src="~/lib/table/datatable/datatables.js"></script>
    <script>
        $('#paymentTable').DataTable({
            "oLanguage": {
                "oPaginate": { "sPrevious": '<svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="feather feather-arrow-left"><line x1="19" y1="12" x2="5" y2="12"></line><polyline points="12 19 5 12 12 5"></polyline></svg>', "sNext": '<svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="feather feather-arrow-right"><line x1="5" y1="12" x2="19" y2="12"></line><polyline points="12 5 19 12 12 19"></polyline></svg>' },
                "sInfo": "عدد النتائج   _TOTAL_",
                "sSearch": '<svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="feather feather-search"><circle cx="11" cy="11" r="8"></circle><line x1="21" y1="21" x2="16.65" y2="16.65"></line></svg>',
                "sSearchPlaceholder": "بحث...",
                "sLengthMenu": "النتائج :  _MENU_ ",
            },
            "order": [[1, "desc"]],
            "stripeClasses": [],
            "lengthMenu": [7, 10, 20, 50],
            "pageLength": 20,
            drawCallback: function () { $('.dataTables_paginate > .pagination').addClass(' pagination-style-13 pagination-bordered mb-5'); }
        });
    </script>
    <!-- END PAGE LEVEL SCRIPTS -->
}